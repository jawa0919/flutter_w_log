name: flutter_w_log
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  stack_trace: ">=1.0.0 <2.0.0"

  sembast: ">=3.0.0 <4.0.0"
  path_provider: ">=2.0.0 <3.0.0"
  path: ">=1.0.0 <2.0.0"
  xxtea: ">=2.0.0 <3.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
